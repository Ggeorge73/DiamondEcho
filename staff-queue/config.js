// Deployment-owned, public configuration. Never place secrets in this file.
// Replace the API origin and matching index.html CSP connect-src before enabling.
window.DIAMOND_ECHO_STAFF_CONFIG = Object.freeze({
  enabled: false,
  apiBase: 'https://api.example.invalid',
});
