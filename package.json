{
  "name": "diamondecho-review-site",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.9.3",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm --prefix frontend run build && node scripts/copy-build.mjs",
    "test": "npm --prefix frontend run test -- --watchAll=false"
  }
}
