{
  "name": "diamondecho-review-site",
  "version": "1.0.0",
  "private": true,
  "packageManager": "npm@10.9.3",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build --workspace frontend && node scripts/copy-build.mjs",
    "test": "npm run test --workspace frontend -- --watchAll=false"
  }
}
